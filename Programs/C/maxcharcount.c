/*

Counting number of characters which are repeated to the
max.

*/

#include<stdio.h>

main() {

  char  *c,*c1,*d,*d1,t ;
  int cntr,n ;

  c = ( *char ) calloc ( size of (char) , 100 ) ;
  d = ( *char ) calloc ( size of (char) , 100 ) ;

  c1 = c ;
  d1 = d ;

  printf("Enter any string : ") ;
  scanf("%[^\n]s",c) ;

  while( *c1 != '\0' ) {

     cntr = 0 ;

     if( *c1 == *(c1 + 1) ) {

        cntr++ ;
     }

    while(  

   
  }

  
} 
